import { _decorator, CCInteger, Component, EventTouch, Graphics, Node, NodeEventType, UITransform } from 'cc';
const { ccclass, property, type, executeInEditMode } = _decorator;

@ccclass('BuildLayer')
export class BuildLayer extends Component {

    start() {
    }

    onEnable() {
    }

    onDisable() {
    }

    update(deltaTime: number) {
        
    }
}


