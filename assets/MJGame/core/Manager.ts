import { _decorator, Component, Node } from 'cc';
import { PlayerID } from './Config';
const { ccclass, property } = _decorator;

@ccclass('Manager')
export class Manager extends Component {

    playerID:PlayerID

    start() {

    }

    update(deltaTime: number) {
        
    }
}


